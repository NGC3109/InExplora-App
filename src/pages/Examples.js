import React, { useEffect } from 'react';
import { View, Text } from 'react-native';
import io from 'socket.io-client';

const TestSocket = () => {

  return (
    <View>
      <Text>Prueba de Socket</Text>
    </View>
  );
};

export default TestSocket;
